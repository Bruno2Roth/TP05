@{
    ViewData["Title"] = "Sala 5";
}
<section class="sala5">
    <h1 class="consigna">En seguridad hay muchos QR, pero sólo uno te dice la clave que abre la puerta</h1>

    <a href='@Url.Action("IrPista", "Home")' class="bPista">Obtener pista</a>
    <ul>
        @foreach(string qr in ViewBag.qrs)
        {
            <li><img src="@Url.Content(qr)" alt="ERROR"></li>
        }
    </ul>
    <form action='@Url.Action("PasarSala", "Home")' method="get" class="form-contraseña" required>
        Contraseña: <input type="text" name="contraseña"/>
        <input type="submit" value="Enviar">
    </form>
</section>
